<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Colors!</title>
    <style>
        body {
            font-family: sans-serif;
            padding: 0;
            margin: 0;
            background-color: black;
            display: flex;
            flex-wrap: wrap;
            gap: 1px;
        }

        div {
            display: flex;
            font-size: xx-small;
            width: 50px;
            height: 50px;
            justify-content: center;
            align-items: center;
            flex-grow: 1;
        }
    </style>
</head>
<body>
    <% step = 0x001000 %>
    <% (0..0xFFFFFF).step(step).each do |i| %>
        <% color = format('#%06X', i) %>
        <div style="background-color: <%= color %>;"><%= color %></div>
    <% end %>
</body>
</html>